#include <stdio.h>
/**
 * main- write anything about this program printing size of types of file
 * Return: return always something nul
 */

int main(void)
{
	printf("the sz of a char:%lu byte", sizeof(char));
	printf("the sz of a int: %lu byte", sizeof(int));
	printf("the sz of a int: %lu byte", sizeof(long int));
	printf("the sz of a int: %lu byte", sizeof(long long int));
	printf("the sz of a int: %lu byte", sizeof(float));
}
